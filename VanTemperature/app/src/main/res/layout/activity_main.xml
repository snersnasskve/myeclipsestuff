<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/celcius_button2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/deg_f"
        android:textSize="36sp"
        app:layout_constraintBaseline_toBaselineOf="@+id/celcius_button"
        app:layout_constraintEnd_toEndOf="@+id/edit_temperature" />

    <EditText
        android:id="@+id/edit_temperature"
        style="@android:style/Widget.Material.EditText"
        android:layout_width="312dp"
        android:layout_height="125dp"
        android:ems="10"
        android:hint="@string/enter_temp"
        android:inputType="number"
        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        android:textSize="36sp"
        app:layout_constraintBottom_toTopOf="@+id/celcius_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:importantForAutofill="no"
        />

    <Button
        android:id="@+id/celcius_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="448dp"
        android:text="@string/deg_c"
        android:textSize="36sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@+id/edit_temperature" />

</androidx.constraintlayout.widget.ConstraintLayout>